# 'projeto crud agendamento de tarefas 3000'

objetivo: agendar tarefas
tecnologias: php, html, sql
estrutura: sei la

para instalar é so instalar

para configurar o banco de dados é so ter um banco e dados

para executar no xampp é so ligar o apache e MySql

para acessar o sistema é so acessar o sistema

teste

print("sucesso")
